# Vela www agent instructions

This repository owns `vela.space` as a single static landing hero.

- Keep only `/`, `src/index.html`, `src/styles.css`, the retained painting, and
  the Vela mark.
- Do not add links, routes, essays, products, explanatory copy, sections,
  components, JavaScript, fonts, tokens, animation, authentication, or data.
- The Vela mark is rights-reserved. The painting is project-owned under the
  content license.
- Use Bun only. Run `bun run verify` before release and inspect the page at
  desktop, 768px, 414px, 375px, and 320px.
- Deployment and domain changes require action-time authorization.
