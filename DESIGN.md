# Vela landing hero

The page is one full-bleed indigo watercolor and one small modern Vela lockup.
It uses the system sans stack, no bundled font, no design tokens, no animation,
and no component system. It never scrolls.
