{
  "name": "@vela-science/www",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.12",
  "engines": {
    "bun": ">=1.3.12"
  },
  "scripts": {
    "build": "bun scripts/build.mjs",
    "check": "bun scripts/check.mjs",
    "test": "bun test",
    "verify": "bun run check && bun run test && bun run build && git diff --check",
    "serve": "bun scripts/serve.mjs"
  }
}
